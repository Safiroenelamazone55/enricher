PORT=3001
NODE_ENV=development

# Max leads per batch upload
BATCH_LIMIT=500

# DNS lookup timeout ms
DNS_TIMEOUT=4000

# MX cache TTL minutes
MX_CACHE_TTL_MIN=60

# Rate limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100
